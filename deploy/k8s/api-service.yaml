apiVersion: v1
kind: Service
metadata:
  name: ai-resume-api
  namespace: ai-resume
  labels:
    app: ai-resume-api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: ai-resume-api
