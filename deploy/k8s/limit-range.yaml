apiVersion: v1
kind: LimitRange
metadata:
  name: ai-resume-defaults
  namespace: ai-resume
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 50m
        memory: 64Mi
      default:
        cpu: 300m
        memory: 256Mi
